# Companion 0.2.4

- Keep the menu bar responsive while Companion quits.
- Show a native macOS spinner during startup and shutdown.
- Show Quitting Companion with disabled text and gently pulse the eyes while closing, respecting Reduce Motion.
- Prevent delayed status updates and settings commands from overriding shutdown.
