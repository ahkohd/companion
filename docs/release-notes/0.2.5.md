# Companion 0.2.5

- Fix a shutdown hang that could prevent Companion from quitting or relaunching during updates.
- Keep startup status refreshing while the menu is open.
- Use disabled text for the startup status, matching shutdown.
