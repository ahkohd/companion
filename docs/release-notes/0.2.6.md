# Companion 0.2.6

- Close Roon connections promptly so quitting no longer waits for a five-second timeout.
- Cancel pending app settings requests during shutdown.
- Show Connecting to device while USB opens and the device responds, instead of briefly showing Disconnected.
- Show Browser only when USB is intentionally disabled.
