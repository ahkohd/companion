# Companion 0.2.3

- Add the new Companion app icon, with layered artwork and a fallback for older macOS versions.
- Default fresh installations to Automatic USB detection so a compatible device connects without choosing a port.
- Keep existing saved connection preferences.
